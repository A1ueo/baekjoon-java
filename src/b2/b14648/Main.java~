package b2.b14648;

import java.io.*;

public class Main {
    public static void main(String[] args) throws Exception {
        BufferedReader br = new BufferedReader(new InputStreamReader(System.in));
        String buff = br.readLine();
        int n = buff.charAt(0) - '0';
        int q = buff.charAt(1) - '0';
        
        buff = br.readLine();
        int[] arr = new int[n];
        for (int i = 0; i < n; i++) {
            arr[i] = buff.charAt(i) - '0';
        }

        int[][] queries = new int[q][];
        for (int i = 0; i < q; i++) {
            buff = br.readLine();
            if (buff.charAt(j) == '1') {
                for (int j = 0; j < buff.length(); j += 3) {
                        int num1 = arr[buff.charAt(j + 1) - '0';
                        int num2 = arr[buff.charAt(j + 1) - '0';
                        System.out.println(num1 + num2);
                        arr[buff.charAt(j + 1)] = num2;
                        arr[buff.charAt(j + 1)] = num2;
                }
            } else if (buff.charAt(j) == '1') {
                
            }
        }

    }
}
